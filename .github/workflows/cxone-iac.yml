
name: Checkmarx IaC + Terraform Plan (PR)

on:
  pull_request:
    branches: [ main ]   # adjust to your default branch

permissions:
  contents: read
  pull-requests: write
  issues: write
  security-events: write   # needed to upload SARIF

jobs:
  iac-source-scan:
    name: IaC Source Scan (CxOne)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node (needed by some actions)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run CxOne IaC scan (source)
        uses: checkmarx/ast-github-action@8e887bb93dacc44e0f5b64ee2b06d5815f89d4fc
        with:
          # --- Auth & endpoint (OAuth) ---
          base_uri:       ${{ secrets.CX_BASE_URI }}
          cx_tenant:      ${{ secrets.CX_TENANT }}
          cx_client_id:   ${{ secrets.CX_CLIENT_ID }}
          cx_client_secret: ${{ secrets.CX_CLIENT_SECRET }}
          additional_params: --scan-types iac-security -s . --report-format sarif --output-name cx-iac-source.sarif --output-path .

          # --- Scan configuration ---
          project_name:   ${{ secrets.CX_PROJECT_NAME }}
          file_source:    .                  # scan repository root
      
      - name: Verify SARIF exists
        run: ls -la cx-iac-source.sarif || (echo "SARIF missing" && exit 1)

      - name: Upload SARIF (source)
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: cx-iac-source.sarif

  iac-plan-scan:
    name: Terraform Plan Scan (CxOne)
    runs-on: ubuntu-latest
    needs: [ iac-source-scan ]
    env:
      TF_IN_AUTOMATION: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6   # pin your version

      # ---- provider credentials go here ----
      # e.g., for AWS:
      - name: Configure AWS creds
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            us-east-1

      # Create a terraform plan file that CxOne can scan
      - name: Terraform init & plan
        run: |
          terraform init -input=false
          terraform plan -input=false -out=tfplan.binary
          terraform show -json tfplan.binary > tfplan.json

      - name: Run CxOne IaC scan (Terraform plan)
        uses: Checkmarx/ast-github-action@8e887bb93dacc44e0f5b64ee2b06d5815f89d4fc
        with:
          # --- Auth & endpoint (OAuth) ---
          base_uri:       ${{ secrets.CX_BASE_URI }}
          cx_tenant:      ${{ secrets.CX_TENANT }}
          cx_client_id:   ${{ secrets.CX_CLIENT_ID }}
          cx_client_secret: ${{ secrets.CX_CLIENT_SECRET }}
          additional_params: --scan-types iac-security -s tfplan.json --report-format sarif --output-name cx-iac-plan.sarif --output-path .

          # --- Plan scan inputs ---
          project_name:   ${{ secrets.CX_PROJECT_NAME }}

          # --- Results output ---
          output_format:   sarif
          output_filename: cx-iac-plan.sarif
  
      - name: Verify Plan SARIF exists
        run: ls -la cx-iac-plan.sarif || (echo "Plan SARIF missing" && exit 1)

      - name: Upload SARIF (plan)
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: cx-iac-plan.sarif
